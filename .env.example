# Print World — Environment Variables (names only; never commit real values)

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
OPENAI_API_KEY=
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# Server-only (required for uploads, admin operations, order creation)
SUPABASE_SERVICE_ROLE_KEY=

# Optional — used for sitemap/robots canonical URLs in production
NEXT_PUBLIC_SITE_URL=
